# Crypto Prediction - core runtime dependencies.
# `pip install -r requirements.txt` is the ONLY install step needed:
# pretrained models ship in models/ and the data layer works offline,
# so the app runs straight after install.
#
# Optional extras (sentiment enrichment): requirements-optional.txt

pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
yfinance>=0.2.30
requests>=2.31.0
torch>=2.1.0
xgboost>=2.0.0
statsmodels>=0.14.0
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
python-dotenv>=1.0.0
joblib>=1.3.0
tqdm>=4.66.0
plotly>=5.17.0
streamlit>=1.28.0
websockets>=12.0
lightgbm>=4.0.0
